import './style.css'
// Side-effect import: registers the <texturize-filter> custom element.
import './canvas-filter.js'

// eslint-disable-next-line no-console
console.log(`Texturizer build ${__APP_VERSION__} (${__BUILD_TIME__})`)
