{
  "name": "texturizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev --proxy 5173 -- npm run dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name texturizer"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "wrangler": "^4.20.0"
  }
}
